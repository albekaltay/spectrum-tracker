

export const temperatureFormatter = (temperature: number) =>
  temperature?.toFixed(2) + " " + "°C";

export const velocityFormatter = (velocity: number) =>
  velocity?.toFixed(2) + " " + "m/s";

export const altitudeFormatter = (altitude: number) =>
  altitude?.toFixed(2) + " " + "m";

