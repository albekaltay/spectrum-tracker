const ROOTS = {
  DASHBOARD: "",
};

// ----------------------------------------------------------------------

export const paths = {
  dashboard: {
    root: `${ROOTS.DASHBOARD}/`,
    spectrum_ws: `${ROOTS.DASHBOARD}/spectrum-ws`,
    }

  
};
